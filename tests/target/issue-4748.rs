fn foo(x: Option<i32>) -> i32 {
    match y {
        Some(y) => y,
        None => 0,
    }
}

fn foo(x: Option<i32>) -> i32 {
    match y {
        Some(y) => y,
        None => 0,
    }
}

fn foo() {
    foo.find(|y| x);
}

fn foo() {
    foo.find(|y| x);
}

fn foo() {
    try { y }
}
