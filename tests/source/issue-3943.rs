use ::foo;
use ::foo::{Bar1};
use ::foo::{Bar2, Baz2};
use ::{Foo};
use ::{Bar3, Baz3};

use ::foo;
use ::foo::Bar;
use ::foo::{Bar, Baz};
use ::Foo;
use ::{Bar, Baz};

use ::Foo;
use ::foo;
use ::foo::Bar;
use ::foo::{Bar, Baz};
use ::{Bar, Baz};
