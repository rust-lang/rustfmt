[package]
name = "ci-integration"
version = "0.0.1"
edition = "2024"
publish = false

[workspace]

[lints.clippy]
# Ensures that `cargo clippy` will never return any errors
# See rustfmt-clippy alias in .cargo/config.toml for lints used for rustfmt development
all = "allow"
